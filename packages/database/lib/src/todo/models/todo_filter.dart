/// Filter options for displaying todos.
enum TodoFilter {
  /// Show all todos regardless of completion status.
  all,

  /// Show only active (uncompleted) todos.
  active,

  /// Show only completed todos.
  completed,
}
