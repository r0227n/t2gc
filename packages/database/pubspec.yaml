name: database
description: DuckDB database integration with Todo and Category models for the Flutter template project.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.10.0
  flutter: 3.38.5

resolution: workspace

dependencies:
  dart_duckdb: ^1.4.4
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.7.1

  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.3
  json_serializable: ^6.11.2
  very_good_analysis: ^10.1.0
